-Installed PHP form net
-Installed composer from net

-composer init (comando apra iniciar el composer, nada no cambiable luego)

- cambiamos el psr- a /app
- una vez ejecutado, en el json, cambiamos el nombre largo automatico a App//

-usamos -> composer dump-autoload / genera el buscador automatico de clases de PHP // 
  Por qué se llama "dump"?
  Porque:
  👉 “vuelca” (dump) la configuración en archivos optimizados

  ⚠️ Cuándo tienes que ejecutarlo?

  Siempre que:

  añades nuevas clases
  cambias namespaces
  modificas autoload en composer.json

- usamos
  php -S (levanta servidor local, importante S mayuscula) localhost:8000 (dirección) -t public (entry point, apunta a nuestra carpeta publica)


